{
  "name": "createFacebookAdCreative",
  "description": "Creates a Facebook ad creative with image and caption. IMPORTANT Workflow:\n1. First, call listPages to get available Facebook pages\n2. Select a page to use for the ad creative\n3. Find the last analyzed image URL from your conversation history (format: https://s3.ap-southeast-2.amazonaws.com/datalis-avatars/uploads/*) - this MUST be the same image you previously used to generate captions\n4. Ask for destination URL (website, product page, WhatsApp link)\n5. Only proceed with creative creation after confirming image URL, page ID, and destination URL are all valid.",
  "strict": true,
  "parameters": {
    "type": "object",
    "required": [
      "ad_account_id",
      "page_id",
      "name",
      "message",
      "link",
      "caption",
      "picture",
      "call_to_action_type"
    ],
    "properties": {
      "ad_account_id": {
        "type": "string",
        "description": "The ID of the ad account. MUST be obtained first through listAccounts command before attempting creative creation."
      },
      "name": {
        "type": "string",
        "description": "The name of the ad creative"
      },
      "page_id": {
        "type": "string",
        "description": "The Facebook Page ID. MUST be obtained first through listPages command."
      },
      "message": {
        "type": "string",
        "description": "Message to be displayed with the link. Must be generated based on the image and confirmed by user."
      },
      "link": {
        "type": "string",
        "description": "Destination URL for the ad. MUST ask user for this. Examples: business website, product page, WhatsApp link (wa.me). Do not use example.com."
      },
      "caption": {
        "type": "string",
        "description": "Caption for the link. Must be generated based on the image and confirmed by user."
      },
      "picture": {
        "type": "string",
        "description": "MUST use the last image URL from conversation history that starts with 'https://s3.ap-southeast-2.amazonaws.com/datalis-avatars/uploads/' - this is the same image you previously analyzed to generate captions."
      },
      "call_to_action_type": {
        "type": "string",
        "description": "Type of the call to action",
        "enum": [
          "SHOP_NOW",
          "LEARN_MORE",
          "SIGN_UP",
          "BOOK_NOW",
          "GET_OFFER",
          "GET_DIRECTIONS"
        ]
      }
    },
    "additionalProperties": false
  }
}
