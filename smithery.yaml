startCommand: npx -y zuckerbot-mcp
configSchema:
  type: object
  properties:
    ZUCKERBOT_API_KEY:
      type: string
      description: "Your ZuckerBot API key"
  required:
    - ZUCKERBOT_API_KEY
